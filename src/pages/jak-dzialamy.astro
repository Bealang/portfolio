---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import HeroBg from "../components/HeroBg.astro";
import ContactForm from "../components/ContactForm.astro";
---

<Layout
    title="Jak działamy | web2sell - Tworzenie stron www Myślenice"
    description="Jak wygląda proces tworzenia strony internetowej w web2sell? Zobacz nasz proces współpracy krok po kroku od pierwszego kontaktu po wdrożenie oraz najczęściej zadawane pytania (FAQ)."
>
    <Header activePage="jak-dzialamy" />

    <main class="zamow-page">
        <HeroBg behindContent={true} />

        <!-- SEKCJA 2: Proces budowania strony — Horizontal Timeline -->
        <section id="zamow-proces" class="zamow-proces">
            <div class="zamow-proces__header reveal-element">
                <h2>
                    Jak wygląda <span class="gradient__text">współpraca</span> z nami?
                </h2>
                <p>
                    Poniżej znajdują się kroki powstawania twojej przyszłej
                    strony internetowej od wysłania formularza, po publikację w
                    sieci.
                </p>
            </div>

            <div class="htl-wrapper">
                <button
                    class="htl-nav htl-nav--prev"
                    id="htlPrev"
                    aria-label="Poprzedni krok"
                >
                    <i class="fa-solid fa-chevron-left"></i>
                </button>

                <div class="htl-viewport" id="htlViewport">
                    <div class="htl-track" id="htlTrack">
                        <!-- Krok 1 -->
                        <div class="htl-step" data-step="1">
                            <div class="htl-step__dot">
                                <span class="htl-step__number">01</span>
                            </div>
                            <div class="htl-step__card">
                                <h3>Kontakt & opis potrzeb</h3>
                                <p>
                                    Wypełniasz formularz kontaktowy, opisując
                                    swoje potrzeby i oczekiwania dotyczące
                                    strony internetowej. Im więcej szczegółów
                                    podasz, tym lepiej będziemy mogli dopasować
                                    ofertę do Twoich wymagań. Przygotuj własne
                                    grafiki (logo, zdjęcia) i opisy, jeśli
                                    chcesz, ale nie jest to konieczne - możemy
                                    też je przygotować za Ciebie.
                                </p>
                            </div>
                        </div>

                        <!-- Krok 2 -->
                        <div class="htl-step" data-step="2">
                            <div class="htl-step__dot">
                                <span class="htl-step__number">02</span>
                            </div>
                            <div class="htl-step__card">
                                <h3>Bezpłatna wycena</h3>
                                <p>
                                    W ciągu <strong>max. 24 godzin</strong> odpowiadamy
                                    na Twoją wiadomość i przedstawiamy naszą wycenę
                                    i szczegóły strony internetowej. Wycena jest bezpłatna
                                    i niezobowiązująca, a wszystkie koszty są jasne
                                    od początku - bez ukrytych opłat. Jeśli masz pytania
                                    dotyczące wyceny lub chcesz wprowadzić zmiany,
                                    jesteśmy otwarci na rozmowę.
                                </p>
                            </div>
                        </div>

                        <!-- Krok 3 -->
                        <div class="htl-step" data-step="3">
                            <div class="htl-step__dot">
                                <span class="htl-step__number">03</span>
                            </div>
                            <div class="htl-step__card">
                                <h3>Szczegóły & materiały</h3>
                                <p>
                                    Po dogadaniu oferty i zaakceptowaniu wyceny,
                                    wysyłasz nam szczegółowe informacje o twojej
                                    stronie:
                                </p>
                                <ul class="htl-step__list">
                                    <li>preferowana kolorystyka</li>
                                    <li>pomysł na układ strony (jeśli masz)</li>
                                    <li>
                                        odpowiednie grafiki (logo w dobrej
                                        jakości, zdjęcia produktów/firmy)
                                    </li>
                                    <li>opisy poszczególnych sekcji</li>
                                </ul>
                            </div>
                        </div>

                        <!-- Krok 4 -->
                        <div class="htl-step" data-step="4">
                            <div class="htl-step__dot">
                                <span class="htl-step__number">04</span>
                            </div>
                            <div class="htl-step__card">
                                <h3>Start pracy & zaliczka</h3>
                                <p>
                                    Zaczynamy pracę nad Twoją stroną. W tym
                                    momencie pobieramy zaliczkę w wysokości <strong
                                        >20%</strong
                                    > wartości zamówienia. Reszta płatna po wykonaniu
                                    i zatwierdzeniu strony. W trakcie pracy będziemy
                                    na bieżąco informować Cię o postępach i konsultować
                                    wszelkie wątpliwości, aby efekt końcowy był zgodny
                                    z Twoimi oczekiwaniami.
                                </p>
                            </div>
                        </div>

                        <!-- Krok 5 -->
                        <div class="htl-step" data-step="5">
                            <div class="htl-step__dot">
                                <span class="htl-step__number">05</span>
                            </div>
                            <div class="htl-step__card">
                                <h3>Prototyp strony</h3>
                                <p>
                                    Po kilku dniach pracy nad projektem,
                                    wysyłamy Ci prototyp strony do wglądu.
                                    Możesz zobaczyć, jak Twoja strona prezentuje
                                    się na żywo, zanim zostanie opublikowana. To
                                    moment, w którym możesz zgłosić swoje uwagi
                                    i sugestie dotyczące wyglądu i
                                    funkcjonalności strony, abyśmy mogli
                                    wprowadzić ewentualne poprawki przed
                                    finalizacją projektu.
                                </p>
                            </div>
                        </div>

                        <!-- Krok 6 -->
                        <div class="htl-step" data-step="6">
                            <div class="htl-step__dot">
                                <span class="htl-step__number">06</span>
                            </div>
                            <div class="htl-step__card">
                                <h3>Tura poprawek</h3>
                                <p>
                                    Po przedstawieniu prototypu, masz możliwość
                                    zgłoszenia <strong
                                        >jednej tury poprawek</strong
                                    >. Wypisujesz wszystkie rzeczy, które chcesz
                                    zmienić, dokładnie i szczegółowo. My
                                    wykonujemy wszystkie poprawki zgodnie z
                                    Twoimi wskazówkami. Jeśli po tej turze
                                    chciałbyś wprowadzić kolejne zmiany,
                                    wyceniamy je indywidualnie (od 50 zł za
                                    drobne korekty)..
                                </p>
                            </div>
                        </div>

                        <!-- Krok 7 -->
                        <div class="htl-step" data-step="7">
                            <div class="htl-step__dot">
                                <span class="htl-step__number">07</span>
                            </div>
                            <div class="htl-step__card">
                                <h3>Hosting & konfiguracja</h3>
                                <p>
                                    Po zaakceptowaniu ostatecznej wersji strony,
                                    zajmujemy się jej publikacją w sieci. Jeśli
                                    nie masz jeszcze hostingu i domeny, możemy
                                    pomóc Ci je założyć i skonfigurować. Jeśli
                                    już posiadasz hosting, zajmiemy się
                                    konfiguracją i wdrożeniem strony na Twoim
                                    serwerze. Po wszystkim strona będzie gotowa
                                    do użytku!
                                </p>
                            </div>
                        </div>

                        <!-- Krok 8 -->
                        <div class="htl-step htl-step--final" data-step="8">
                            <div class="htl-step__dot">
                                <span class="htl-step__number">08</span>
                            </div>
                            <div class="htl-step__card htl-step__card--final">
                                <h3>Gotowe!</h3>
                                <p>
                                    Strona internetowa jest gotowa i
                                    opublikowana! Po zakończeniu projektu,
                                    wysyłamy Ci wszystkie pliki źródłowe strony
                                    oraz krótką dokumentację techniczną.
                                    Jesteśmy też dostępni do pomocy po wdrożeniu
                                    bezpłatnie przez określony czas w <a
                                        href="/cennik"
                                        style="color: var(--accent-color); font-weight: 500;"
                                        >cenniku</a
                                    >, jeśli będziesz potrzebować dodatkowych
                                    zmian lub wsparcia.
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                <button
                    class="htl-nav htl-nav--next"
                    id="htlNext"
                    aria-label="Następny krok"
                >
                    <i class="fa-solid fa-chevron-right"></i>
                </button>
            </div>
            <p class="pricing-note">
                Szczegółowe zasady współpracy znajdziesz <a
                    href="/zasady-wspolpracy">tutaj</a
                >
            </p>
        </section>

        <!-- SEKCJA 3: FAQ -->
        <section id="faq" class="zamow-faq">
            <div class="zamow-faq__header reveal-element">
                <h2>
                    Najczęściej zadawane <span class="gradient__text"
                        >pytania</span
                    >
                </h2>
            </div>

            <div class="faq-list reveal-element">
                <div class="faq-item">
                    <button class="faq-item__question" aria-expanded="false">
                        <span
                            >Czy muszę mieć kupioną domenę i hosting przed
                            kontaktem?</span
                        >
                        <i class="fa-solid fa-chevron-down faq-item__chevron"
                        ></i>
                    </button>
                    <div class="faq-item__answer">
                        <p>
                            <strong>Nie.</strong> Nie trzeba, gdyż wszystko konfigurujemy
                            od zera. Jesteśmy w stanie także przenieść stronę na inny
                            hosting. Jeśli masz starą stronę — nie ma problemu, zajmiemy
                            się migracją.
                        </p>
                    </div>
                </div>

                <div class="faq-item">
                    <button class="faq-item__question" aria-expanded="false">
                        <span>Ile trwa realizacja?</span>
                        <i class="fa-solid fa-chevron-down faq-item__chevron"
                        ></i>
                    </button>
                    <div class="faq-item__answer">
                        <p>
                            Realizacja prostej wizytówki trwa zazwyczaj około <strong
                                >od 1 do 2 tygodni</strong
                            >. Profesjonalne systemy zarządzania treścią (blog,
                            aktualności, łatwo edytowane galerie itd.) z pakietu <strong
                                >System</strong
                            > mają czas realizacji co najmniej <strong
                                >od 3 tygodni</strong
                            >.
                            <br />
                            Wstępny czas realizacji dla każdego pakietu jest podany
                            w <a
                                href="/cennik"
                                style="color: var(--accent-color); font-weight: 500;"
                                >cenniku</a
                            >.
                        </p>
                    </div>
                </div>

                <div class="faq-item">
                    <button class="faq-item__question" aria-expanded="false">
                        <span>Czy stroną będę mógł zarządzać sam?</span>
                        <i class="fa-solid fa-chevron-down faq-item__chevron"
                        ></i>
                    </button>
                    <div class="faq-item__answer">
                        <p>
                            Po zakończonej pracy wysyłamy kod źródłowy strony i
                            krótką dokumentację techniczną. Dla prostych
                            wizytówek nie ma dedykowanego panelu do edytowania
                            wartości na stronie. Natomiast dla pakietu <strong
                                >System z własnym CMS</strong
                            >
                            — oczywiście, można zarządzać samemu stroną. Trzeba tylko
                            dać znać przy składaniu zamówienia, co chcesz łatwo edytować.
                        </p>
                    </div>
                </div>
                <div class="faq-item">
                    <button class="faq-item__question" aria-expanded="false">
                        <span
                            >Jakie są koszty utrzymania strony internetowej?</span
                        >
                        <i class="fa-solid fa-chevron-down faq-item__chevron"
                        ></i>
                    </button>
                    <div class="faq-item__answer">
                        <p>
                            Koszt utrzymania strony zależy od wybranego pakietu.
                            Najprostsze wizytówki i proste systemy CMS dla
                            lokalnych firm wymagają hostingu w cenie od ~137zł
                            netto rocznie. Jeśli strona zacznie się zacinać,
                            wolno działać, można bez problemu przejść na droższy
                            i wydajniejszy pakiet. Natomiast każda strona
                            internetowa wymaga domeny .pl - dla przykładu
                            twojanazwa.pl, której koszt to 99zł netto rocznie.
                            <br />
                            Zarejestrowanie domeny na pierwszy rok: <b
                                >~15zł netto</b
                            ><br />
                            Zarejestrowanie hostingu na pierwszy rok: <b
                                >~50zł netto</b
                            >
                        </p>
                    </div>
                </div>
                <div class="faq-item">
                    <button class="faq-item__question" aria-expanded="false">
                        <span>Czy mogę otrzymać fakturę?</span>
                        <i class="fa-solid fa-chevron-down faq-item__chevron"
                        ></i>
                    </button>
                    <div class="faq-item__answer">
                        <p>
                            Po zakończeniu realizacji projektu wystawiamy
                            fakturę VAT na całą kwotę za pośrednictwem serwisu <a
                                href="https://useme.pl"
                                target="_blank"
                                rel="noopener noreferrer">useme.pl</a
                            >. Dzięki temu masz pewność, że wszystko jest w 100%
                            legalne i zgodne z przepisami podatkowymi.
                            <br />
                            <b>Jak to wygląda w praktyce?</b> My zgłaszamy zlecenie
                            w serwisie, a Ty otrzymujesz na maila fakturę proforma
                            do opłacenia. Nie musisz zakładać żadnego konta ani się
                            rejestrować. Po zaksięgowaniu wpłaty system automatycznie
                            wyśle do Ciebie oficjalną fakturę VAT, którą bez problemu
                            wrzucisz w koszty firmy. Jeśli masz pytania dotyczące
                            rozliczenia, daj nam znać!
                        </p>
                    </div>
                </div>
            </div>
        </section>

        <!-- SEKCJA 5: CTA + Formularz kontaktowy -->
        <section id="zamow-kontakt" class="zamow-kontakt">
            <div class="zamow-kontakt__header reveal-element">
                <h2>
                    Gotowy na <span class="gradient__text">współpracę?</span>
                </h2>
            </div>
            <div class="reveal-element">
                <ContactForm />
            </div>
            <div class="google-map-embed">
                <iframe
                    src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d489350.3443682875!2d19.536384132661276!3d49.958665938616434!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x6447d991c045ed81%3A0x80337cb291998748!2sweb2sell!5e1!3m2!1spl!2spl!4v1779286812460!5m2!1spl!2spl"
                    width="100%"
                    height="400"
                    style="border:0; display:block;"
                    allowfullscreen=""
                    loading="lazy"
                    referrerpolicy="no-referrer-when-downgrade"
                    title="Lokalizacja web2sell na mapie Google"></iframe>
            </div>
        </section>
    </main>

    <Footer />

    <!-- Horizontal Timeline -->
    <script is:inline>
        (function () {
            var viewport = document.getElementById("htlViewport");
            var prevBtn = document.getElementById("htlPrev");
            var nextBtn = document.getElementById("htlNext");
            var steps = viewport.querySelectorAll(".htl-step");

            // Smooth inertia scroll interpolator (extremely responsive, zero stutter)
            var targetLeft = viewport.scrollLeft;
            var currentLeft = viewport.scrollLeft;
            var isAnimating = false;

            function updateActiveStep() {
                var viewportCenter = viewport.scrollLeft + viewport.clientWidth / 2;
                var activeStep = null;
                var minDiff = Infinity;

                steps.forEach(function (step) {
                    var stepCenter = step.offsetLeft + step.offsetWidth / 2;
                    var diff = Math.abs(stepCenter - viewportCenter);
                    if (diff < minDiff) {
                        minDiff = diff;
                        activeStep = step;
                    }
                });

                steps.forEach(function (step) {
                    if (step === activeStep) {
                        step.classList.add("htl-step--active");
                    } else {
                        step.classList.remove("htl-step--active");
                    }
                });
            }

            function smoothScrollUpdate() {
                var diff = targetLeft - currentLeft;
                if (Math.abs(diff) > 0.5) {
                    currentLeft += diff * 0.12; // Easing factor for buttery smooth motion
                    viewport.scrollLeft = currentLeft;
                    updateActiveStep();
                    requestAnimationFrame(smoothScrollUpdate);
                } else {
                    viewport.scrollLeft = targetLeft;
                    currentLeft = targetLeft;
                    updateActiveStep();
                    isAnimating = false;
                }
            }

            viewport.addEventListener(
                "wheel",
                function (e) {
                    if (e.deltaY !== 0) {
                        var maxScrollLeft =
                            viewport.scrollWidth - viewport.clientWidth;
                        var expectedTarget = targetLeft + e.deltaY * 1.3;

                        // Only intercept vertical scroll if we are not at the boundaries
                        if (
                            (e.deltaY > 0 && targetLeft < maxScrollLeft - 2) ||
                            (e.deltaY < 0 && targetLeft > 2)
                        ) {
                            e.preventDefault();

                            targetLeft = Math.max(
                                0,
                                Math.min(expectedTarget, maxScrollLeft),
                            );

                            if (!isAnimating) {
                                isAnimating = true;
                                currentLeft = viewport.scrollLeft; // Sync to current scroll position
                                requestAnimationFrame(smoothScrollUpdate);
                            }
                        }
                    }
                },
                { passive: false },
            );

            // Synchronize targetLeft and active step on other scroll interactions (native touch, drag, buttons)
            viewport.addEventListener("scroll", function () {
                if (!isAnimating) {
                    targetLeft = viewport.scrollLeft;
                }
                updateActiveStep();
                updateArrows();
            });

            // Drag-to-scroll functionality (1:1 responsive drag, zero lag)
            var isDown = false;
            var startX;
            var scrollLeftVal;

            viewport.addEventListener("mousedown", function (e) {
                isDown = true;
                viewport.classList.add("is-dragging");
                startX = e.pageX - viewport.offsetLeft;
                scrollLeftVal = viewport.scrollLeft;
            });

            viewport.addEventListener("mouseleave", function () {
                isDown = false;
                viewport.classList.remove("is-dragging");
            });

            viewport.addEventListener("mouseup", function () {
                isDown = false;
                viewport.classList.remove("is-dragging");
            });

            viewport.addEventListener("mousemove", function (e) {
                if (!isDown) return;
                e.preventDefault();
                var x = e.pageX - viewport.offsetLeft;
                var walk = (x - startX) * 1.5; // multiplier for fast scroll response
                viewport.scrollLeft = scrollLeftVal - walk;
            });

            function getActiveIndex() {
                for (var i = 0; i < steps.length; i++) {
                    if (steps[i].classList.contains("htl-step--active")) {
                        return i;
                    }
                }
                return 0;
            }

            function scrollToStep(index) {
                var targetStep = steps[index];
                if (!targetStep) return;

                var maxScrollLeft = viewport.scrollWidth - viewport.clientWidth;
                var targetScroll = targetStep.offsetLeft - (viewport.clientWidth - targetStep.offsetWidth) / 2;
                targetLeft = Math.max(0, Math.min(targetScroll, maxScrollLeft));

                if (!isAnimating) {
                    isAnimating = true;
                    currentLeft = viewport.scrollLeft;
                    requestAnimationFrame(smoothScrollUpdate);
                }
            }

            prevBtn.addEventListener("click", function () {
                var currentIndex = getActiveIndex();
                var targetIndex = Math.max(0, currentIndex - 1);
                scrollToStep(targetIndex);
            });

            nextBtn.addEventListener("click", function () {
                var currentIndex = getActiveIndex();
                var targetIndex = Math.min(steps.length - 1, currentIndex + 1);
                scrollToStep(targetIndex);
            });

            // Arrow buttons visibility check
            function updateArrows() {
                var maxScrollLeft = viewport.scrollWidth - viewport.clientWidth;

                if (viewport.scrollLeft <= 5) {
                    prevBtn.style.opacity = "0.3";
                    prevBtn.style.pointerEvents = "none";
                } else {
                    prevBtn.style.opacity = "1";
                    prevBtn.style.pointerEvents = "all";
                }

                if (viewport.scrollLeft >= maxScrollLeft - 5) {
                    nextBtn.style.opacity = "0.3";
                    nextBtn.style.pointerEvents = "none";
                } else {
                    nextBtn.style.opacity = "1";
                    nextBtn.style.pointerEvents = "all";
                }
            }

            window.addEventListener("resize", function () {
                updateActiveStep();
                updateArrows();
            });

            // Initial setup
            setTimeout(function () {
                updateActiveStep();
                updateArrows();
            }, 150);
        })();
    </script>

    <!-- FAQ Accordion -->
    <script is:inline>
        (function () {
            var questions = document.querySelectorAll(".faq-item__question");
            questions.forEach(function (btn) {
                btn.addEventListener("click", function () {
                    var item = btn.closest(".faq-item");
                    var isOpen = item.classList.contains("is-open");

                    // Close all others
                    document
                        .querySelectorAll(".faq-item.is-open")
                        .forEach(function (openItem) {
                            openItem.classList.remove("is-open");
                            openItem
                                .querySelector(".faq-item__question")
                                .setAttribute("aria-expanded", "false");
                        });

                    // Toggle current
                    if (!isOpen) {
                        item.classList.add("is-open");
                        btn.setAttribute("aria-expanded", "true");
                    }
                });
            });
        })();
    </script>

    <!-- Scroll Reveal Animations Script -->
    <script is:inline>
        (function () {
            // 1. Activate Hero animation immediately
            var hero = document.getElementById("zamow-hero");
            if (hero) {
                setTimeout(function () {
                    hero.classList.add("zamow-hero--active");
                }, 50);
            }

            // 2. Setup IntersectionObserver for vertical reveal elements
            var revealElements = document.querySelectorAll(".reveal-element");
            if ("IntersectionObserver" in window && revealElements.length > 0) {
                var verticalObserver = new IntersectionObserver(
                    function (entries, observer) {
                        entries.forEach(function (entry) {
                            if (entry.isIntersecting) {
                                entry.target.classList.add("reveal--visible");
                                observer.unobserve(entry.target);
                            }
                        });
                    },
                    {
                        rootMargin: "0px 0px -80px 0px", // triggers slightly before entering viewport fully
                        threshold: 0.05,
                    },
                );

                revealElements.forEach(function (el) {
                    verticalObserver.observe(el);
                });
            } else {
                // Fallback for older browsers
                revealElements.forEach(function (el) {
                    el.classList.add("reveal--visible");
                });
            }

            // 3. Setup IntersectionObserver for Horizontal Timeline Steps (Option B)
            var steps = document.querySelectorAll(".htl-step");
            var htlViewport = document.getElementById("htlViewport");

            if (
                "IntersectionObserver" in window &&
                steps.length > 0 &&
                htlViewport
            ) {
                var timelineObserver = new IntersectionObserver(
                    function (entries) {
                        entries.forEach(function (entry) {
                            if (entry.isIntersecting) {
                                entry.target.classList.add("htl-step--visible");
                            } else {
                                // Elements fade/scale back when scrolled out for visual dynamism
                                entry.target.classList.remove(
                                    "htl-step--visible",
                                );
                            }
                        });
                    },
                    {
                        root: htlViewport,
                        rootMargin: "0px -40px 0px -40px", // shrink horizontal bound slightly so cards animate as they near edges
                        threshold: 0.15,
                    },
                );

                steps.forEach(function (step) {
                    timelineObserver.observe(step);
                });
            } else {
                // Fallback for older browsers
                steps.forEach(function (step) {
                    step.classList.add("htl-step--visible");
                });
            }
        })();
    </script>
</Layout>
